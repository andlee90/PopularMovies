<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingTop="10dp"
    xmlns:tools="http://schemas.android.com/tools">

    <ImageView
        android:id="@+id/iv_author"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_alignParentLeft="true"
        android:layout_marginStart="8dp"
        android:layout_marginLeft="8dp"
        android:layout_marginRight="12dp"
        android:layout_marginEnd="12dp"
        android:src="@drawable/ic_display_author"
        android:contentDescription="@string/review_item_content_desc" />

    <TextView
        android:id="@+id/tv_author"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@id/iv_author"
        android:layout_toEndOf="@+id/iv_author"
        android:paddingTop="8dp"
        android:paddingBottom="4dp"
        android:textColor="@color/colorText"
        android:textStyle="bold"
        tools:text="author" />

    <TextView
        android:id="@+id/tv_content"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/iv_author"
        android:paddingBottom="12dp"
        android:textColor="@color/colorText"
        tools:text="content" />
</RelativeLayout>